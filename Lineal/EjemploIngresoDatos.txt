# ARCHIVO DE ENTRADA PARA PROBLEMA DE PROGRAMACIÓN LINEAL
# Formato: eliminar todos los comentarios (líneas con #) antes de usar

# Línea 1: Tipo de problema - Escribir 'max' para maximización o 'min' para minimización
max

# Línea 2: Número de variables de decisión - Escribir solo el número
2

# Línea 3: Coeficientes de la función objetivo
# Escribir los coeficientes separados por espacios
# Ejemplo: Para la función 2x₁ + 4x₂, escribir:
2 4

# Línea 4: Número de restricciones - Escribir solo el número
4

# Líneas siguientes: Restricciones
# Formato: coeficientes [espacios] signo valor
# Signos válidos: <= (menor igual), >= (mayor igual), = (igual)
# Ejemplo: Para 1x₁ + 3x₂ <= 400, escribir:
1 3 <= 400
1 1 <= 200
3 0 >= 40
0 1 >= 20

# EJEMPLO DE PROBLEMA COMPLETO SIN COMENTARIOS:
# max
# 2
# 2 4
# 4
# 1 3 <= 400
# 1 1 <= 200
# 3 0 >= 40
# 0 1 >= 20